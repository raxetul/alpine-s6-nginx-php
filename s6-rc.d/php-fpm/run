#!/command/with-contenv sh
echo "STARTED SERVICE: php-fpm"
## PHP_VERSION comes from the image env (see Dockerfile ARG/ENV); with-contenv
## makes it available here. -F keeps php-fpm in the foreground for s6.
exec php-fpm${PHP_VERSION} -F
